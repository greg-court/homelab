apiVersion: apps/v1
kind: Deployment
metadata:
  name: netbird
spec:
  selector:
    matchLabels:
      app: netbird
      egress.zone: fam
  template:
    metadata:
      labels:
        app: netbird
        egress.zone: fam
    spec:
      containers:
        - name: netbird
          env:
            - name: NB_SETUP_KEY
              valueFrom:
                secretKeyRef:
                  name: netbird-setup-fam
                  key: setup-key
            - name: NB_HOSTNAME
              value: netbird-fam
            - name: NB_WIREGUARD_PORT
              value: '31826'

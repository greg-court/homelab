apiVersion: apps/v1
kind: Deployment
metadata:
  name: gh-packer-runner
  namespace: agents-and-runners
spec:
  replicas: 2
  selector:
    matchLabels: { app: gh-packer-runner }
  template:
    metadata:
      labels: { app: gh-packer-runner }
    spec:
      containers:
        - name: runner
          image: docker.io/gr10/gh-packer-runner:latest
          envFrom:
            - secretRef:
                name: gh-runners-env
          env:
            - name: RUNNER_LABELS
              value: 'self-hosted,packer'
          resources: {}
